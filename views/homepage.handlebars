{{!-- <div class="mb-5 pt-3">
{{#each posts as |post|}}

{{> post-info post }}
</div>

{{else}}
<h6 class="pb-4 has-text-centered is-italic">there are currently no posts</h6>
{{/each}} --}}

    {{#each posts as |post| }}
        {{> post-info }}
    {{/each}}
